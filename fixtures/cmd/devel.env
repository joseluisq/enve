## MySQL Export - Fixture settings
## -------------------------------

# Connection settings (optional)
DB_PROTOCOL=tcp
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DEFAULT_CHARACTER_SET=utf8

# Database settings (required)
DB_NAME="dbname"
DB_USERNAME="username"
DB_PASSWORD="passwd"

# GZip export file (optional)
DB_EXPORT_GZIP=true

# SQL or Gzip export file (optional).
# If `DB_IMPORT_GZIP` is `true` then file name should be `database_name.sql.gz`
DB_EXPORT_FILE_PATH="$DB_NAME.sql.gz"

# Additional arguments (optional)
DB_ARGS=
